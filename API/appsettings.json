{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "Kafka": {
        "BootstrapServers": "localhost:9092", // Kafka server address
        "GroupId": "my-consumer-group", // Consumer group ID
        "Topic": "test-topic" // Default topic
    }
}
